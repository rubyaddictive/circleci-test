version: 2.0
jobs:
  build:
    docker:
      - image: alpine:3.7
    working_directory: ~/myapp
    branches:
      only: master
    steps:
      - run:
          name: The first step
          command: |
            echo 'Hello, World'
            echo 'This is the delivery pipeline'
